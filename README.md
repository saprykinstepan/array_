#Работа с массивами в C
## Лабораторная 11
Задания:

Заполнение массива с консоли (10 элементов)

Преобразование: возведение каждого элемента в квадрат

Печать в виде таблицы

Вычисление среднего арифметического

Анализ массива: сумма положительных/отрицательных, их количество

## Лабораторная 14
Задания:

Заполнение массива значениями из функции f(x) = e^(-0.1x) * sin(x)

Печать массива (по 5 элементов в строке)

Вычисление суммы элементов

Поиск элемента в массиве

Нахождение максимума до позиции K

## Лабораторная 16
Задания:

Генерация массива случайными числами

Поиск max/min элементов

Вычисление среднего значения

Подсчет элементов выше среднего

## Лабораторная 19
Задания:

Работа с файлами (создание, запись, чтение)

Табулирование функции f(x) = sin(x) * cos(x)

Интерактивное меню выбора операций

 Структура проекта
text
lab-arrays/
├── app.c        # Главная программа
├── arrays.h     # Заголовочный файл
├── arrays.c     # Реализации функций
└── Makefile     # Скрипт сборки
🔧 Основные функции
double* full_elements(double *ptr_array, int n)
Заполнение массива значениями функции
Параметры: ptr_array - указатель на массив, n - размер
Возвращает: указатель на заполненный массив

int put_elements(double *ptr_array, int n)
Печать элементов массива
Возвращает: количество выведенных элементов

double* calc_elements(double *ptr_array, int n)
Обработка массива (возведение в квадрат)

double sum_elements(double *ptr_array, int begin, int end)
Вычисление суммы элементов в диапазоне

int find_element(double *ptr_array, int n, double element)
Поиск элемента в массиве
Возвращает: индекс или -1
